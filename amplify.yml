version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ng update @angular/cli @angular/core
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/front-end
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
