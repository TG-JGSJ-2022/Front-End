import { ImagePredictionResponse } from './image-prediction-response';

describe('ImagePredictionResponse', () => {
  it('should create an instance', () => {
    expect(new ImagePredictionResponse()).toBeTruthy();
  });
});
