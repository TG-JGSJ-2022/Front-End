import { ResponseInterfaces } from './response-interfaces';

describe('ResponseInterfaces', () => {
  it('should create an instance', () => {
    expect(new ResponseInterfaces()).toBeTruthy();
  });
});
